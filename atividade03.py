"""Atividade 03:
Imprima em tela a sequência de
0 até 50."""

contador = 0

while contador <= 50:
    print(contador) #Imprimimos o próprio contador e a cada vez que ele passa pelo loop, assume um novo valor com o incremento.
    contador = contador + 1 #INCREMENTO!